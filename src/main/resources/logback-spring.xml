<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- Absolute path so the Datadog Agent can tail reliably -->
  <property name="LOG_DIR" value="C:/jmeter/logs"/>
  <property name="LOG_FILE" value="${LOG_DIR}/app.log"/>

  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <!-- include MDC keys from Datadog agent -->
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg | service=healthcare-service trace_id=%X{dd.trace_id} span_id=%X{dd.span_id}%n</pattern>
    </encoder>
  </appender>

  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_FILE}</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/app-%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>30</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg | service=healthcare-service trace_id=%X{dd.trace_id} span_id=%X{dd.span_id}%n</pattern>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="FILE"/>
  </root>
</configuration>
