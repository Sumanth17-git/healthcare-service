server.port=8081
spring.application.name=healthcare-service
# Optional: set log level if you want more chatty logs
logging.level.com.healthcare=INFO
# Expose common actuator endpoints for health checks
management.endpoints.web.exposure.include=health,info,metrics,env
